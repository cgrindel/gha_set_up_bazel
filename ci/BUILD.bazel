load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

sh_binary(
    name = "foo",
    srcs = ["foo.sh"],
)

build_test(
    name = "build_test",
    targets = [
        ":foo",
    ],
)
